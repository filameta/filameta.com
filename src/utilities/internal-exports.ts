import { useDoc, useDocById, useDocsSidebar } from "@docusaurus/theme-common/internal"

// Re-export these "kind of" internal API's.
// This is mostly so intellisense works correctly.

export {
    useDoc,
    useDocById,
    useDocsSidebar
}
